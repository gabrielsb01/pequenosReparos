import React from 'react'
import { IProps } from './interfaces/damage.interface'
import Damage from './containers/damage.container'

const DamageMain = (props: IProps ) => {
    return <Damage {...this.props} />
}

export default DamageMain